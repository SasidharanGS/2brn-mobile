// Global Jest setup. @testing-library/react-native v14 auto-registers its Jest
// matchers when the library is imported, so no `extend-expect` import is needed.
// Add global native-module mocks here as the suite grows.
export {}
