# dependencies
node_modules/

# expo
.expo/
dist/
web-build/
# NOTE: expo-env.d.ts is intentionally committed (see the file) so fresh
# checkouts type-check without first running the Expo dev server.

# native (generated by `expo prebuild`)
android/
ios/
*.apk
*.aab
*.keystore
*.jks

# release signing secret (local upload key + passwords) — never commit; keep only the .example
# (use credentials/* not credentials/ so git still descends and the negation can re-include the example)
credentials/*
!credentials/keystore.properties.example

# metro / bundler
.metro-health-check*

# misc
.DS_Store
*.log
.env*.local
coverage/

# agent / editor local config (never commit — personal)
AGENTS.md
.claude/
.opencode/
